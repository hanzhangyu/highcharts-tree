{
  "name": "highcharts-tree",
  "version": "0.0.1",
  "description": "Create tree type in Highcharts",
  "main": "lib/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "dev": "tsc --watch & parcel example/index.html",
    "build": "tsc",
    "lint": "tslint --project \"./tsconfig.json\"",
    "ut": "tsc && ava --verbose --color",
    "test": "npm run lint && npm run ut"
  },
  "keywords": [
    "highcharts",
    "tree",
    "dendrogram"
  ],
  "author": "zhangyu.han@outlook.com",
  "license": "MIT",
  "peerDependencies": {
    "highcharts": "^5.0.0"
  },
  "devDependencies": {
    "@types/highcharts": "^5.0.38",
    "ava": "^1.3.1",
    "parcel-bundler": "^1.12.2",
    "prettier": "^1.16.4",
    "ts-node": "^8.0.3",
    "tslint": "^5.14.0",
    "typescript": "^3.3.3333"
  },
  "ava": {
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register/transpile-only"
    ]
  },
  "dependencies": {
    "highcharts": "^5.0.0"
  }
}
